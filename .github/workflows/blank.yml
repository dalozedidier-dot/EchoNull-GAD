name: template-disabled

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled template workflow
        run: |
          echo "This workflow was auto-generated by GitHub templates and is intentionally disabled."
          echo "Use 'tests' and 'Release' workflows instead."
