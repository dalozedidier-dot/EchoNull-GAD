name: tests

on:
  push:
  pull_request:
  workflow_dispatch:

concurrency:
  group: tests-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install lint tooling
        shell: bash
        run: |
          python -m pip install -U pip
          pip install pre-commit

      - name: Run pre-commit (all files)
        shell: bash
        run: |
          pre-commit run --all-files

  unit:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: "pip"

      - name: Install minimal deps
        shell: bash
        run: |
          python -m pip install -U pip
          pip install pytest pytest-cov
          # minimal runtime deps for current tests
          pip install numpy pandas networkx tqdm

      - name: Run tests (coverage only on 3.12)
        shell: bash
        env:
          PYTHONPATH: ${{ github.workspace }}
        run: |
          if [ "${{ matrix.python-version }}" = "3.12" ]; then
            pytest -q --cov=echonull --cov-report=term-missing --cov-report=xml
          else
            pytest -q
          fi

      - name: Upload coverage artifact (3.12 only)
        if: always() && matrix.python-version == '3.12'
        uses: actions/upload-artifact@v4
        with:
          name: coverage-xml
          path: coverage.xml
          if-no-files-found: warn
          retention-days: 14
